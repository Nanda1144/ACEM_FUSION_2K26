# MongoDB Atlas Configuration
# DO NOT commit your actual .env file to version control.
# Set these variables in your deployment platform (e.g., Vercel Dashboard).

# Connection string (Server-side ONLY)
MONGODB_URI=mongodb+srv://username:password@cluster.xxxxx.mongodb.net/acem_db?retryWrites=true&w=majority

# Database name (Server-side ONLY)
MONGODB_DB_NAME=acem_db

# API Configuration
VITE_API_URL=/api
PORT=5000

# Instructions:
# 1. Copy this file to .env
# 2. Add your actual MONGODB_URI and MONGODB_DB_NAME
# 3. For local development, npm run dev:full will use these values.
# 4. For Vercel, add these keys in Project Settings > Environment Variables.
